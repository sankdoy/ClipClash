Cloudflare Pages Functions

This folder contains Pages Functions routed under `/api/*`.

Note: Durable Objects are not defined here; DOs must be deployed as a separate Worker and bound into Pages later.
